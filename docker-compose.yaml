
services:
  # api: 
  #   build: ./api
  #   container_name: api_c
  #   ports:
  #       - '4000:4000'
  #   volumes:
  #       - ./src:/app
  #       - ./app/node_modules

  portfolio:
    build: .
    container_name: portfolio_c
    ports:
      - '3000:3000'
    stdin_open: true
    tty: true
